apiVersion: v1
kind: Pod
metadata:
  name: good-pvc-volume
spec:
  containers:
    - name: app
      image: alpine:3.20
      volumeMounts:
        - name: data
          mountPath: /data
  volumes:
    - name: data
      persistentVolumeClaim:
        claimName: app-data
