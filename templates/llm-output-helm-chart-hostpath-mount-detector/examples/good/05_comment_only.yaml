apiVersion: v1
kind: Pod
metadata:
  name: good-comment-only
spec:
  # NOTE: do not add a hostPath volume here -- breaks pod isolation.
  # Previously had: hostPath: { path: /etc }  -- removed.
  containers:
    - name: app
      image: alpine:3.20
  volumes:
    - name: scratch
      emptyDir: {}
