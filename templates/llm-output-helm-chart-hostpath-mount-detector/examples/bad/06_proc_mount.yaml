apiVersion: v1
kind: Pod
metadata:
  name: bad-proc-mount
spec:
  containers:
    - name: snoop
      image: alpine:3.20
      volumeMounts:
        - name: proc
          mountPath: /host-proc
          readOnly: true
  volumes:
    - name: proc
      hostPath:
        path: /proc
        type: Directory
