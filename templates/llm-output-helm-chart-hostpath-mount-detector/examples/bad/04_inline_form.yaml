apiVersion: v1
kind: Pod
metadata:
  name: bad-inline-form
spec:
  containers:
    - name: app
      image: alpine:3.20
      volumeMounts:
        - name: etc
          mountPath: /host-etc
  volumes:
    - name: etc
      hostPath: { path: /etc, type: Directory }
