[core]
dags_folder = /opt/airflow/dags
executor = LocalExecutor
# fernet_key generated locally with `python -c "from cryptography.fernet
# import Fernet; print(Fernet.generate_key().decode())"` and stored in
# the platform secret manager; mounted at runtime via env var, not here.
sql_alchemy_conn_cmd = /usr/local/bin/get-airflow-db-uri
