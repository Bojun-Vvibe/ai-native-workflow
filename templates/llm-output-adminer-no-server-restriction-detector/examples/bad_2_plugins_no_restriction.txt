# Explicit 0.0.0.0 bind, ADMINER_PLUGINS only loads the cosmetic
# "tables-filter" plugin (no restriction).
services:
  db-ui:
    image: adminer:latest
    environment:
      ADMINER_PLUGINS: "tables-filter dump-json"
      ADMINER_DESIGN: "nette"
    ports:
      - "0.0.0.0:8081:8080"
