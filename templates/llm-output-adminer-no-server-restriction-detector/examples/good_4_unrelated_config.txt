# Unrelated config: nginx reverse proxy. No Adminer markers at all.
services:
  web:
    image: nginx:1.27
    ports:
      - "80:80"
    volumes:
      - ./default.conf:/etc/nginx/conf.d/default.conf:ro
