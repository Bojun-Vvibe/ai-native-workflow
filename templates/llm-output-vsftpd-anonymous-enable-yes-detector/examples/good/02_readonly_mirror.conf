# /etc/vsftpd.conf — read-only anonymous (acceptable for a public mirror),
# explicitly denying every form of anonymous write.
listen=YES
anonymous_enable=YES
anon_upload_enable=NO
anon_mkdir_write_enable=NO
anon_other_write_enable=NO
write_enable=NO
local_enable=NO
no_anon_password=NO
