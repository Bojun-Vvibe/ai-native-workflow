FROM python:3.11-slim
RUN pip install --no-cache-dir OctoPrint
EXPOSE 5000
# Default access control on; first-run wizard prompts for admin on first hit
CMD ["octoprint", "serve", "--host", "127.0.0.1", "--port", "5000"]
