# client config — basic auth credentials wired from a properties source
schema.registry.url=https://schema-registry:8081
basic.auth.credentials.source=USER_INFO
schema.registry.basic.auth.user.info=svc-consumer:${SR_PASSWORD}
