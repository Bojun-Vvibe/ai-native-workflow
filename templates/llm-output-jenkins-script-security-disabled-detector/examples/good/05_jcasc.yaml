# JCasC YAML that approves only specific, narrow signatures (no
# wildcard). This is the safe pattern.
security:
  scriptApproval:
    approvedSignatures:
      - "method java.lang.String length"
      - "staticMethod java.lang.Math max int int"
