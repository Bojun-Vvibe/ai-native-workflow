# Good sample 3: documentation file mentioning the knob only
# inside `#` comments. Variable not actually set.
# Uptime Kuma config knobs (excerpt):
#   UPTIME_KUMA_DISABLE_AUTH=true
#     -> disables the login screen entirely. ONLY for kiosk
#        deployments behind a trusted reverse proxy that performs
#        its own authentication. Never expose to the LAN with
#        this set.
UPTIME_KUMA_PORT=3001
UPTIME_KUMA_HOST=127.0.0.1
