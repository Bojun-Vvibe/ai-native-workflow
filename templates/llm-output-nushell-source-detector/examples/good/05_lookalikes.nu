#!/usr/bin/env nu
# good/05_lookalikes.nu — words that look like `source` / `nu` but
# aren't the keyword token at command position.
let resource = "x"
let nuget = "y"
print $resource
print $nuget
let sourcecode = "literal"
print $sourcecode
