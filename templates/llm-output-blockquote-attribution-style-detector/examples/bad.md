# Notes on craft (with formatting issues)

The first quote uses an em-dash inside the blockquote — fine by itself.

> The function of good software is to make the complex appear to be simple.
>
> — Grady Booch

The second quote puts the attribution OUTSIDE the blockquote, which renders
as a separate list item rather than an attribution.

> Premature optimization is the root of all evil.

-- Donald Knuth

The third quote uses a double-hyphen attribution INSIDE the block, mixing
styles with the first quote.

> Simplicity is prerequisite for reliability.
>
> -- Edsger Dijkstra

The fourth quote has an orphan attribution dash with no author text.

> Talk is cheap. Show me the code.
>
> —
