server.document-root = "/var/www/html"
server.port = 80

# NOTE: do NOT set `dir-listing.activate = "enable"` here — we
# explicitly want directory browsing off so dotfiles don't leak.
index-file.names = ( "index.html" )
