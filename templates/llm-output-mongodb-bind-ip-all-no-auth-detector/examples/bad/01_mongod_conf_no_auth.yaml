# /etc/mongod.conf — exposes every interface, no auth block at all.
storage:
  dbPath: /var/lib/mongo
net:
  port: 27017
  bindIp: 0.0.0.0
systemLog:
  destination: file
  path: /var/log/mongodb/mongod.log
  logAppend: true
