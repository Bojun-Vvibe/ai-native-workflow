# Bad: docker-compose exposes 7700 with no master key env at all
version: "3.8"
services:
  search:
    image: getmeili/meilisearch:v1.10
    ports:
      - "7700:7700"
    environment:
      MEILI_ENV: "production"
    volumes:
      - meili_data:/meili_data
volumes:
  meili_data:
