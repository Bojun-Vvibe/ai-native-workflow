apiVersion: v1
kind: ConfigMap
metadata:
  name: vault-config
data:
  vault.hcl: |
    ui = true
    disable_mlock = true
    storage "raft" {
      path = "/vault/data"
      node_id = "node-1"
    }
    listener "tcp" {
      address = "0.0.0.0:8200"
      tls_disable = 0
    }
