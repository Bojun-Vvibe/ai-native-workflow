apiVersion: v1
kind: Secret
metadata:
  name: outline-secrets
  annotations:
    outlinewiki/outline: managed-by-eso
type: Opaque
stringData:
  SECRET_KEY: "{{ .Values.outline.secretKey }}"
  UTILS_SECRET: "{{ .Values.outline.utilsSecret }}"
