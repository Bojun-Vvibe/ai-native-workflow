apiVersion: apps/v1
kind: Deployment
metadata:
  name: pushgateway
spec:
  template:
    spec:
      containers:
        - name: pushgateway
          image: prom/pushgateway:v1.9.0
          args:
            - "--web.listen-address=0.0.0.0:9091"
          ports:
            - containerPort: 9091
