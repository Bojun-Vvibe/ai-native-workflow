# knot.conf — good: defaults only, no acl entries that grant update.
server:
    listen: 0.0.0.0@53

acl:
  - id: ro
    address: 198.51.100.0/24
    action: transfer

zone:
  - domain: example.org
    file: "/var/lib/knot/example.org.zone"
    acl: ro
