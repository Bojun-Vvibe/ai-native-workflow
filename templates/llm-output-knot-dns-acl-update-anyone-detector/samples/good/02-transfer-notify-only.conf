# knot.conf — good: only transfer/notify granted, no update action.
server:
    listen: 0.0.0.0@53

acl:
  - id: secondaries
    address: 192.0.2.0/24
    action: [transfer, notify]

zone:
  - domain: example.net
    file: "/var/lib/knot/example.net.zone"
    acl: secondaries
