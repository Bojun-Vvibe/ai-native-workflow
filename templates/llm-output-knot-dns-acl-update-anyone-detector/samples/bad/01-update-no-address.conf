# knot.conf — bad: acl grants `update` with no address restriction.
# RFC 2136 dynamic updates from anywhere on the internet.
server:
    listen: 0.0.0.0@53

acl:
  - id: allow_update
    action: update

zone:
  - domain: example.com
    file: "/var/lib/knot/example.com.zone"
    acl: allow_update
