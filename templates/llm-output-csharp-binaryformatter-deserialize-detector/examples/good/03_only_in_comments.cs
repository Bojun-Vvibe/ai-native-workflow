// This file mentions BinaryFormatter only in comments and docs.
// new BinaryFormatter().Deserialize(stream);  // <- not real code, just a doc string
public class Doc {
    /* Once upon a time we used BinaryFormatter.Deserialize here. */
    public int Answer() { return 42; }
}
