# Title

Here is a code block with trailing blank lines inside the fence:

```python
def hello():
    print("hi")


```

And another, with whitespace-only blank lines:

```
some text

   
```

This one is fine:

```python
ok = True
```

But this one is not — single trailing blank:

```js
console.log("x");

```
