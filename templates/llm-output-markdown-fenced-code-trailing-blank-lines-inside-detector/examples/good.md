# Title

A clean fenced code block, no trailing blanks:

```python
def hello():
    print("hi")
```

Another, with content right up to the close:

```
plain text content
final line
```

And one with internal blank lines (these are fine — only *trailing* blanks
just before the closing fence are flagged):

```js
const a = 1;

const b = 2;
console.log(a + b);
```
