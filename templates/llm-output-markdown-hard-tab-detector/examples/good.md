# Title

A clean paragraph with only spaces for indentation.

- item one
  - nested with two spaces
- item two

Inside a fenced code block, tabs are intentional and allowed:

```make
target:
	echo "this tab is fine inside the fence"
```

End of document.
