#!/usr/bin/env xonsh
# 03_string_literal_with_word_execx.xsh — the WORD execx appears
# inside a quoted help string, not as a call.
help_text = "use execx(SOURCE) to run a literal snippet"
print(help_text)
