version: "3.8"
services:
  rspamd:
    image: rspamd/rspamd:latest
    secrets:
      - rspamd_controller_password
    environment:
      - REDIS_HOST=redis
secrets:
  rspamd_controller_password:
    file: ./secrets/rspamd_controller_password.hash
