protocols = imap pop3
listen = *
mail_location = maildir:~/Maildir

ssl = no
auth_mechanisms = plain
disable_plaintext_auth = no

service pop3-login {
    inet_listener pop3 {
        port = 110
    }
}
