# Pure TCP load balancer — no http {} block, no version banner exposure.
events { worker_connections 1024; }

stream {
    upstream pgsql {
        server 10.0.0.10:5432;
        server 10.0.0.11:5432;
    }

    server {
        listen 5432;
        proxy_pass pgsql;
    }
}
