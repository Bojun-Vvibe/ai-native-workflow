# Uses ngx_http_headers_more to drop the Server header entirely.
events { worker_connections 1024; }

http {
    more_clear_headers Server;

    server {
        listen 80;
        server_name app.example.com;
        location / { return 200 "ok"; }
    }
}
