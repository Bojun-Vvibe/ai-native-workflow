events { worker_connections 1024; }

http {
    server_tokens build;
    sendfile on;

    server {
        listen 80;
        server_name app.example.com;
        location / { return 200 "ok"; }
    }
}
