# Unrelated — postgres docker compose, contains the word "api"
# in a comment but is not PowerDNS.
version: "3.8"
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_PASSWORD: "changeme"
    ports:
      - "127.0.0.1:5432:5432"
    # internal api=yes flag for app, unrelated
