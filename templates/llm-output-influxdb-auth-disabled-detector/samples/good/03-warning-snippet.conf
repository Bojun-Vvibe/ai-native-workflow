# Documentation snippet that *warns* against the insecure default.
# auth-enabled = false   # NEVER set this — disables HTTP authentication
[http]
  enabled = true
  auth-enabled = true
  bind-address = "127.0.0.1:8086"
[admin]
  # legacy admin UI removed in 1.3+; do not enable
  enabled = false
