# /etc/redis/redis.conf — ACL surface looks "configured" but default user
# can authenticate with no credential and has full access.
port 6379
bind 0.0.0.0
protected-mode no
user default on nopass ~* &* +@all
