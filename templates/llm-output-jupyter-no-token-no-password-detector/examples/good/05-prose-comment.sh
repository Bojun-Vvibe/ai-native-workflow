# README snippet — prose only, no real assignment.
# When Jupyter complains about a token, do NOT just set token = '' and
# password = '' to silence it. Use `jupyter server password` instead.
echo "see docs"
