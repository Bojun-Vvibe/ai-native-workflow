# Pure include fragment: only Alias / Directory rules, no top-level
# server directives. Should NOT be flagged for missing
# ServerSignature/ServerTokens because hardening belongs in the
# parent httpd.conf, not in every fragment.
Alias /static/ "/var/www/static/"

<Directory "/var/www/static">
    Require all granted
    Options -Indexes
</Directory>
