; A non-Gitea INI file that happens to have a [security] section.
; Should NOT be flagged — no Gitea fingerprint anywhere.
[server]
listen = 0.0.0.0:8080

[security]
require_tls = true
allow_anonymous = false
