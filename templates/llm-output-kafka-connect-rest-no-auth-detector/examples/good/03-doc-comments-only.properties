# Documentation file. The "bad" example below appears only inside
# `#` comments and inside fenced code blocks meant for instruction;
# the detector should treat this as a comment-only file and stay
# silent.
#
# Example INCORRECT config (do NOT use):
#   rest.host.name=0.0.0.0
#   listeners=http://0.0.0.0:8083
#
# Example CORRECT config:
#   rest.host.name=127.0.0.1
#
# (No live key=value lines below; everything is commented out.)
