acl office_net src 192.168.10.0/24
acl staff proxy_auth REQUIRED

# `all` here is narrowed by office_net AND staff — the AND-join restricts.
http_access allow all office_net staff
http_access deny all
