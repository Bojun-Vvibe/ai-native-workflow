uid = rsync
gid = rsync

[uploads]
path = /srv/uploads
read only = false
auth users = alice, bob
secrets file = /etc/rsyncd.secrets

[downloads]
path = /srv/downloads
read only = true
auth users = reader
secrets file = /etc/rsyncd.secrets
