# Some references

See [the docs](https://example.com/docs) for more.

Also check [the api](<https://example.com/api>) which uses angle brackets
because the URL has nothing weird but the author prefers the form.

A code span with brackets is fine: `[x](<not-a-link)`.

```
[x](<also not a link, in a fence)
```

End of file.
