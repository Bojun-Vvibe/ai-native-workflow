server {
    # The dangerous form below is intentionally commented out so the
    # detector should NOT flag it.
    # location /static { alias /var/www/static/; }

    location /static/ {
        alias /var/www/static/;
    }
}
