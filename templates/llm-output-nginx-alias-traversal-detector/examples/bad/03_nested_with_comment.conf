http {
    server {
        location /assets {
            # serve from disk
            alias /opt/app/assets/;
            expires 30d;
        }

        location /api/ {
            proxy_pass http://backend;
        }
    }
}
