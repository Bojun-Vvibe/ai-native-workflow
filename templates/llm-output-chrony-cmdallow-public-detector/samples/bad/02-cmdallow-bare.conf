# chrony.conf — bad: cmdallow with no argument (chrony parses this
# as an unconditional allow, identical to `cmdallow all`)
pool pool.ntp.org iburst
driftfile /var/lib/chrony/drift
makestep 1.0 3
rtcsync

cmdallow
