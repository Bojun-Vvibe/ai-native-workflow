# chrony.conf — good: defaults (no cmdallow / no bindcmdaddress).
# chrony binds the control socket to localhost and denies remote
# commands by default.
pool pool.ntp.org iburst
driftfile /var/lib/chrony/drift
makestep 1.0 3
rtcsync
