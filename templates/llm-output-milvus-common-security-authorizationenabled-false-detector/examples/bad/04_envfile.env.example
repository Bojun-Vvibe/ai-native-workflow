# Bad: docker .env override — image entrypoint accepts both
# AUTHORIZATION_ENABLED and AUTHORIZATIONENABLED spellings.
# FAKE values; do not use.
ETCD_USE_EMBED=true
COMMON_STORAGETYPE=local
MILVUS_COMMON_SECURITY_AUTHORIZATION_ENABLED=false
