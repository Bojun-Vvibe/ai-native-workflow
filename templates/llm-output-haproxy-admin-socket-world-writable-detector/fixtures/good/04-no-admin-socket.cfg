global
    log /dev/log local0
    # No admin socket at all — stats are exposed only via the HTTP frontend
    daemon

frontend stats
    bind 127.0.0.1:8404
    stats enable
    stats uri /stats
    stats auth admin:supersecret
