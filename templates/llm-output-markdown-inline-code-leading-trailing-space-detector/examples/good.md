# Notes

The function `foo` returns 42, while `bar` is deprecated.

The literal `` ` `` represents a single backtick (legitimate single-space pad).

Inside a fenced block we ignore everything:

```
some ` weird ` text inside a fence
```

After the fence, `qux` is clean.
