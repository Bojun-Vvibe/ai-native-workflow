version: "3.8"
services:
  rethinkdb:
    image: rethinkdb:2.4
    # Public binding is intentional, but the admin password is set on
    # first boot so the HTTP admin UI is not anonymous.
    command: rethinkdb --bind all --initial-password s3cret-bootstrap
    ports:
      - "8080:8080"
