<!-- The user element is present but the password was templated
     from a missing env var, so it serialises as empty. Syncthing's
     basic-auth handler accepts any password against the empty
     stored value. -->
<configuration version="37">
  <gui enabled="true" tls="false">
    <address>[::]:8384</address>
    <user>admin</user>
    <password></password>
    <apikey>some-key</apikey>
  </gui>
</configuration>
