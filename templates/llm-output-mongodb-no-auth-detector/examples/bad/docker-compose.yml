# Bad: docker-compose mongo service with no root creds and no --auth.
version: "3.8"
services:
  db:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
