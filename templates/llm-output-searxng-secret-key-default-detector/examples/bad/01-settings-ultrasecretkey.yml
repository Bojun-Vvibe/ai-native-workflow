# SearXNG settings.yml — verbatim from the upstream tutorial.
use_default_settings: true

server:
  port: 8888
  bind_address: "0.0.0.0"
  secret_key: "ultrasecretkey"
  base_url: false
  image_proxy: true
