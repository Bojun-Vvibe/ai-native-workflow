; clean ini: every section header appears exactly once
[tox]
envlist = py311

[testenv]
deps =
    pytest
    pytest-cov
commands = pytest --cov

[testenv:lint]
deps = ruff
commands = ruff check .

[flake8]
max-line-length = 100
extend-ignore = E203
