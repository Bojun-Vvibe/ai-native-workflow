#!/usr/bin/env yash
# bad/08_eval_brace_param.yash — ${var} brace-form parameter
# expansion inside a double-quoted eval argument. Still dynamic.
tail="; rm -rf /tmp/work"
eval "echo running ${tail}"
