# Properly secured forward listener with shared_key.
<source>
  @type forward
  port 24224
  bind 0.0.0.0
  <security>
    self_hostname log-aggregator-01
    shared_key ${FLUENTD_SHARED_KEY}
  </security>
</source>

<match **>
  @type stdout
</match>
