# `<security>` block exists but only sets self_hostname -- shared_key
# is conspicuously absent.
<source>
  @type forward
  port 24224
  <security>
    self_hostname log-aggregator-01
    user_auth false
  </security>
</source>
