# /etc/rabbitmq/rabbitmq.conf
# No loopback override of any kind — upstream default applies.
listeners.tcp.default = 5672
management.tcp.port   = 15672

# Operator provisions a non-guest admin out of band:
#   rabbitmqctl add_user ops $(pwgen -s 32 1)
#   rabbitmqctl delete_user guest
