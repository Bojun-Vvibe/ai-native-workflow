<!-- DRAFT — not posted to upstream -->

# Draft review: anomalyco/opencode#23965

**PR**: [feat(tui): add provider disconnect command](https://github.com/anomalyco/opencode/pull/23965)
**Author**: @kzekiue
**Size**: +179 / -0 across 3 files
**Recommendation**: `approve` (with one clarifying question)
**Confidence**: medium

## Summary

Adds a `provider disconnect` command to the TUI that lets the user remove a configured provider from local state without editing the config file by hand. Implementation adds a new command handler, wires it into the command palette, and adds confirmation prompt before destructive action.

## Risk areas

- **Blast radius**: low. Touches only TUI command-handler files and a small slice of provider config state. No protocol or storage-format changes.
- **Test coverage delta**: no new tests added. The command path is short, but the confirmation-prompt branch and the "provider not found" error branch are both testable without a real provider connection.
- **Reversibility**: the command mutates local config. PR includes a confirmation prompt. No undo, but config is in version control for users following the recommended setup, so the blast radius is bounded.

## Suggested clarifying questions

1. Is there an intentional reason no test was added? The command-palette wiring usually has a small unit test in this repo — happy to point at the existing pattern if that helps.
2. What happens if the user disconnects the only configured provider? Does the next prompt loop show a sensible empty state, or does it crash on a missing default?

## Recommended action

Approve once question 2 is answered. The change is small, scoped, and matches existing TUI command conventions. A test would be nice-to-have, not a blocker for a feature this small.
