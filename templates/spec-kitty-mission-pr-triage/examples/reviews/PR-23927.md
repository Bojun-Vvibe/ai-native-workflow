<!-- DRAFT — not posted to upstream -->

# Draft review: anomalyco/opencode#23927

**PR**: [fix(provider): preserve Bedrock Claude reasoning replay](https://github.com/anomalyco/opencode/pull/23927)
**Author**: @PershingSquare
**Size**: +74 / -2 across 2 files
**Recommendation**: `request-changes` (small, but needs a test)
**Confidence**: high

## Summary

Fixes a regression where Bedrock-hosted Claude responses lost their reasoning trace on session replay. The fix preserves the reasoning blocks through the provider adapter's normalization step. Diff is small but the surface — provider adapter normalization — is historically a regression hotspot.

## Risk areas

- **Blast radius**: scoped to the Bedrock provider adapter, but adapter normalization touches every assistant message that passes through the provider. A bug here doesn't crash; it silently drops or corrupts content.
- **Test coverage delta**: no test added. Given the prior regression history on this exact normalization path, a regression test is the difference between "fixed forever" and "fixed until next refactor."
- **Cross-provider implications**: if other providers (Anthropic direct, Vertex) share normalization helpers, the change should be checked for parity. From the diff this looks Bedrock-only, but worth confirming.

## Suggested clarifying questions

1. Could you add a fixture-based test for the reasoning replay path? Even a minimal "given Bedrock response with reasoning blocks, after normalization the blocks are intact" assertion would lock this in.
2. Was the original regression introduced by a specific PR? If so, linking it in the description helps future archaeology.

## Recommended action

Request changes. The fix itself looks correct; the missing test is the issue. This is exactly the kind of silent-corruption bug that returns six months later if there's no regression test pinning the behavior.
