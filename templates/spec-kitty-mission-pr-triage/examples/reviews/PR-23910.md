<!-- DRAFT — not posted to upstream -->

# Draft review: anomalyco/opencode#23910

**PR**: [docs: clarify agent variants and built-in presets](https://github.com/anomalyco/opencode/pull/23910)
**Author**: @jjangga0214
**Size**: +415 / -87 across 23 files
**Recommendation**: `comment`
**Confidence**: medium

## Summary

Documentation pass that clarifies the distinction between agent variants and the built-in agent presets, updates examples, and tightens cross-references across the docs site. Touches 23 files but no code; the change is largely additive prose plus some restructuring of existing sections.

## Risk areas

- **Blast radius**: docs only. Zero runtime risk. Risk is reader-facing: if the new framing contradicts how the code actually behaves, users get confused, not broken.
- **Cross-file consistency**: 23 files is a lot of surface for a docs PR. Worth a spot-check that terminology is identical across files (e.g. is it always "agent variant" or does "agent flavor" sneak in anywhere?).
- **Example drift**: examples in docs tend to drift from current code behavior. If any of the touched examples reference command flags or config keys, those should be re-run against current main to confirm they still work.

## Suggested clarifying questions

None blocking. Two non-blocking observations:

1. Consider whether a glossary entry for "agent variant" vs "preset" would help — the distinction comes up enough that a single canonical definition would prevent future drift.
2. If there's a docs lint or link-check in CI, the result of that on this PR would be useful to surface in the description.

## Recommended action

Comment. Docs PRs of this size benefit from a second reader's eyes on consistency, but there's nothing here that warrants blocking. Approval is reasonable once a maintainer has spot-checked terminology across the touched files.
