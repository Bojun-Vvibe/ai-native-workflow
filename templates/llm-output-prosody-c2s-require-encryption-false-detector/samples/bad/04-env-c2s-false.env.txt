# Docker compose env-file — bad: PROSODY_C2S_REQUIRE_ENCRYPTION=false
# (rendered by an LLM "make Prosody work without TLS for local dev"
# prompt; the file ends up shipped to staging unchanged)
PROSODY_DOMAIN=example.org
PROSODY_ADMINS=admin@example.org
PROSODY_C2S_REQUIRE_ENCRYPTION=false
PROSODY_S2S_REQUIRE_ENCRYPTION=true
