# Clean document — all URLs properly wrapped

Visit the spec at <https://example.com/spec> for details.

Inline link form: see [the docs](https://example.org/docs) for the API.

Reference style: jump to [the release][rel] for changes.

[rel]: https://example.org/release

URLs inside code spans are fine: `https://example.com/api`.

```sh
curl https://example.com/api
```

URLs inside fenced code blocks are also fine.
