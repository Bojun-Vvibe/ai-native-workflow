[main]
certname = puppet.internal.example
environment = production

[master]
dns_alt_names = puppet,puppet.internal.example
# Policy autosigner: shell script decides per CSR by inspecting
# the embedded pre-shared key in the CSR attributes.
autosign = /etc/puppetlabs/puppet/autosign.sh
report = true
