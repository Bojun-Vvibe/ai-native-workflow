## /etc/rabbitmq/rabbitmq.conf
## "We had trouble connecting from another container so we removed
## the loopback restriction" — classic LLM-suggested fix.
listeners.tcp.default = 5672
loopback_users = none
