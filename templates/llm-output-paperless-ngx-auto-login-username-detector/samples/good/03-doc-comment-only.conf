# Good sample 3: documentation file that quotes the variable
# only inside a `#` comment, with the variable not actually set.
# Paperless config knobs (excerpt):
#   PAPERLESS_AUTO_LOGIN_USERNAME=foo
#     -> bypass login and treat every request as `foo`.
#     -> ONLY safe behind a trusted reverse proxy that strips
#        the original Host / forwarded headers.
PAPERLESS_URL=https://docs.example.lan
PAPERLESS_SECRET_KEY=a-real-secret-rotated-out-of-band
PAPERLESS_REDIS=redis://broker:6379
PAPERLESS_DBHOST=db
