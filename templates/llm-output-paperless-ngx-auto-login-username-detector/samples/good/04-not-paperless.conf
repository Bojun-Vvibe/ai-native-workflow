# Good sample 4: not a Paperless file. Some unrelated env that
# happens to define a similarly-named variable. Detector requires
# Paperless-specific scope, so this must not fire.
APP_NAME=my-other-tool
DATABASE_URL=postgres://localhost/app
SESSION_SECRET=do-not-share
PAPERLESS_AUTO_LOGIN_USERNAME=admin
