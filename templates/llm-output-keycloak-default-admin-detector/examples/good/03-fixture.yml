# keycloak-default-admin-allowed
# Ephemeral CI fixture for an isolated integration test.
version: "3.8"
services:
  keycloak:
    image: quay.io/keycloak/keycloak:26.0
    command: start-dev
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin
