# helm values for frps chart -- dashboard exposed with no password
bindPort: 7000
vhostHTTPPort: 8080

webServer:
  addr: "0.0.0.0"
  port: 7500
  # (intentionally) no `password:` key below.
  user: "admin"
