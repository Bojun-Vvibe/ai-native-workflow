# A Pillar SLS that happens to mention open_mode in a comment
# explaining why it must NEVER be enabled. The detector ignores
# comment lines.
worker_threads: 5
# open_mode: True  -- forbidden in production; left here as a reminder
