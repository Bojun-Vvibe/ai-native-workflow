version: "3.8"
services:
  vouch-proxy:
    image: quay.io/vouch/vouch-proxy:0.42.0
    ports:
      - "127.0.0.1:9090:9090"
    environment:
      VOUCH_JWT_SECRET: ${VOUCH_JWT_SECRET}
      VOUCH_OAUTH_PROVIDER: google
