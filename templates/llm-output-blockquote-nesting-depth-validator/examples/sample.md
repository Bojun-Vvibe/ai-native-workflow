Here is the model's reply quoting a long chain of sources.

> First level quote starts here.
> > > Skipped a level — depth jump from 1 to 3.

A normal paragraph between quote blocks.

> level one
>> level two no space
> > level two with space
>>> still going

> Above this line is a bare gt with trailing spaces:
>   
>
> Another quote — there is an empty quote line two lines above and a trailing-space line three lines above.
> > > > > Way too deep, depth six.
> > Closing thought at depth two.
This continuation has no blank line and will get sucked into the depth-2 quote.

End of document.
