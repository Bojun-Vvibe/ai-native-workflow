# Legacy single-listener form, but bound to loopback. No TLS is acceptable
# here because the broker is only reachable from the same host.
port 1883
bind_address 127.0.0.1
allow_anonymous false
password_file /etc/mosquitto/passwd
