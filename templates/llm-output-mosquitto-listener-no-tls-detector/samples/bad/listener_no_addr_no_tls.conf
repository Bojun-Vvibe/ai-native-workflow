# Mosquitto listener with no address — binds to every interface by default.
# No cafile / certfile / keyfile, so passwords cross the wire in cleartext.
listener 1883
allow_anonymous false
password_file /etc/mosquitto/passwd
