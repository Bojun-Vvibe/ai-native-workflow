# Legacy single-listener form: top-level `port 1883` with no bind_address
# (so it binds to all interfaces) and no TLS material at top level.
port 1883
allow_anonymous false
password_file /etc/mosquitto/passwd
persistence true
