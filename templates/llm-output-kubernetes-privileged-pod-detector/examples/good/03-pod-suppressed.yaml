apiVersion: v1
kind: Pod
metadata:
  name: legacy-debug
spec:
  containers:
    - name: shell
      image: busybox:1.36
      securityContext:
        # This pod is part of an isolated benchmark cluster; reviewed
        # 2026-04-30 by platform team. See ticket PLAT-7821.
        privileged: true  # llm-allow:privileged-pod
