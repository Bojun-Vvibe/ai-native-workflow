# aerospike.conf — single-node dev cluster
service {
    user root
    group root
    paxos-single-replica-limit 1
    proto-fd-max 15000
}

security {
    enable-security false
}

namespace test {
    replication-factor 1
    memory-size 1G
    storage-engine memory
}
