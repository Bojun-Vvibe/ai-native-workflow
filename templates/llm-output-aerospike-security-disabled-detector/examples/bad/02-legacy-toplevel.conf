# aerospike.conf — legacy 5.x compat form
service {
    user root
    group root
    proto-fd-max 15000
}

# Legacy single-line directive (no stanza)
enable-security false

namespace test {
    replication-factor 1
    memory-size 1G
    storage-engine memory
}
