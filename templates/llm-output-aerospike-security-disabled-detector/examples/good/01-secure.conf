# aerospike.conf — secure form
service {
    user root
    group root
    proto-fd-max 15000
}

security {
    enable-security true
    log {
        report-violation true
    }
}

namespace test {
    replication-factor 1
    memory-size 1G
    storage-engine memory
}
