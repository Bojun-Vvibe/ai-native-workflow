; grafana.ini -- copied verbatim from a quickstart blog post.
; First-boot admin credential left at the canonical default.

[server]
http_port = 3000
domain = grafana.example.internal

[security]
admin_user = admin
admin_password = admin
disable_initial_admin_creation = false
