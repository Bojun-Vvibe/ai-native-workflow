# grafana-default-admin-password-allowed
# Ephemeral CI smoke-test container; torn down at end of job.
version: "3.8"
services:
  grafana-ci:
    image: grafana/grafana:10.4.0
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
