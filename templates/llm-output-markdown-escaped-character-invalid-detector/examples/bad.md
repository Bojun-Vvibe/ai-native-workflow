# Notes on regex

Use \d to match digits and \w for word characters. The path was C:\users\test
and the newline was rendered as \n in the output.

Here is a fenced block where escapes are fine:

```
\d \w \n \t
```

But back in prose, \z is not a thing.
