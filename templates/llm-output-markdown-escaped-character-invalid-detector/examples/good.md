# Valid escapes only

You can escape punctuation: \*not bold\*, \[not a link\], \\backslash, \`code\`.

Inside a fence, anything goes:

```
\d \w \n \t \z
```

Inline code also bypasses: `\d \w \n`.

End of file.
