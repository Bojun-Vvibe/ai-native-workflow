// node-red-adminauth-disabled-allowed
// Local-only smoke fixture; suppression marker present so the
// detector skips this file wholesale.
module.exports = {
    uiPort: 1880,
    flowFile: "flows.json",
    functionGlobalContext: {},
};
