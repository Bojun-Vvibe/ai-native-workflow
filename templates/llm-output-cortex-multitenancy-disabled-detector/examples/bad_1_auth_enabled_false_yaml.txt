# Cortex single-binary "getting started" config — auth disabled.
# This is the shape an LLM emits when asked for "the simplest cortex
# config that works".
target: all
auth_enabled: false

server:
  http_listen_port: 9009
  grpc_listen_port: 9095

ingester:
  lifecycler:
    ring:
      kvstore:
        store: inmemory
      replication_factor: 1

blocks_storage:
  backend: filesystem
  filesystem:
    dir: /tmp/cortex/blocks

compactor:
  data_dir: /tmp/cortex/compactor
  sharding_ring:
    kvstore:
      store: inmemory
