# Runbook: oauth2-proxy operations
#
# Do NOT do this:
#   --skip-auth-regex='^.*$'
#   skip_auth_regex = [ ".*" ]
#
# Doing so disables authentication for the upstream entirely.
#
# Recommended config: only allow /healthz unauthenticated.
#   --skip-auth-regex='^/healthz$'
