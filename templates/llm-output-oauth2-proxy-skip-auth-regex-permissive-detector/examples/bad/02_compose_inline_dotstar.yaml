version: "3.8"
services:
  oauth2-proxy:
    image: quay.io/oauth2-proxy/oauth2-proxy:latest
    command:
      - --http-address=0.0.0.0:4180
      - --upstream=http://backend:9000
      - --provider=oidc
      - --skip-auth-regex=.*
    ports:
      - "4180:4180"
