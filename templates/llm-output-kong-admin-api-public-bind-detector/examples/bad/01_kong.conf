# kong.conf — pasted from a "make Admin API reachable" tutorial.
# This binds the unauthenticated control plane to every interface.
admin_listen = 0.0.0.0:8001, 0.0.0.0:8444 ssl
proxy_listen = 0.0.0.0:8000, 0.0.0.0:8443 ssl
database = postgres
pg_host = kong-db
