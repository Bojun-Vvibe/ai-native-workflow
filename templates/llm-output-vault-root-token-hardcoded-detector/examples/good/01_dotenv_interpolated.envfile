# Operator pulls VAULT_TOKEN from the host environment via shell
# expansion; nothing is baked into the file.
VAULT_ADDR=https://vault.internal.example.com:8200
VAULT_TOKEN=${VAULT_TOKEN}
VAULT_NAMESPACE=admin
LOG_LEVEL=info
