client wifi-controller {
    ipv4addr = 192.168.50.10
    secret = "secret"
    shortname = wlc
}

home_server upstream-radius {
    ipaddr = 10.0.0.5
    port = 1812
    type = auth
    secret = radius
}
