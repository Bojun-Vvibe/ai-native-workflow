# Plain Laravel app config (NOT BookStack). Out of scope for this
# detector. APP_KEY is empty here, but no BookStack signal is present
# and APP_KEY isn't co-located with any canonical APP_URL/DB_* keys
# from the scoping list.
APP_KEY=
DEBUG=true
LOG_LEVEL=info
