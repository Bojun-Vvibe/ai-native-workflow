version: "3.9"
services:
  hardened:
    image: ghcr.io/example/hardened:2.0.0
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    security_opt:
      - "no-new-privileges:true"
    read_only: true
    tmpfs:
      - /tmp
