cert = /etc/stunnel/server.pem
key  = /etc/stunnel/server.key
pid  = /var/run/stunnel.pid

[postgres-tls]
client = no
accept = :15432
connect = db.internal:5432
; intended to be mTLS but verify left unset and no CA configured
