# nginx-autoindex-allowed: internal-only behind auth_basic + IP allowlist
server {
    listen 127.0.0.1:8181;
    location /internal-files/ {
        auth_basic "internal";
        auth_basic_user_file /etc/nginx/.htpasswd;
        allow 10.0.0.0/8;
        deny all;
        autoindex on;
    }
}
