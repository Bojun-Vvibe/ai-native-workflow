server {
    listen 80;
    location /static/ {
        root /srv/static;
        # these alone do nothing without autoindex on;
        autoindex_exact_size on;
        autoindex_localtime on;
        index index.html;
    }
}
