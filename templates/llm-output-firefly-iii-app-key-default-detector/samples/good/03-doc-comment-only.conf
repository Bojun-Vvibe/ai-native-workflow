# Good sample 3: documentation file mentioning APP_KEY only in a
# `#` comment, with the variable not actually set.
# Firefly III config knobs (excerpt):
#   APP_KEY=SomeRandomStringOf32CharsExactly
#     -> DO NOT use the literal value above; generate a real
#        32-byte random string with `php artisan key:generate`.
APP_NAME="Firefly III"
APP_URL=https://firefly.example.lan
SITE_OWNER=mail@example.com
STATIC_CRON_TOKEN=abcdefghijklmnopqrstuvwxyz012345
DB_CONNECTION=mysql
DB_HOST=db
DB_USERNAME=firefly
DB_PASSWORD=firefly
