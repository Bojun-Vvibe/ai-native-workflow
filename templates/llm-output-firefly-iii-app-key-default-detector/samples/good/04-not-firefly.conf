# Good sample 4: not a Firefly III file. Some other Laravel-ish
# app that happens to define APP_KEY at a placeholder. Detector
# requires Firefly-specific scope (image or Firefly-only env keys),
# so this must not fire.
APP_NAME="MyOtherApp"
APP_ENV=production
APP_KEY=SomeRandomStringOf32CharsExactly
DATABASE_URL=postgres://localhost/myotherapp
SESSION_SECRET=do-not-share
