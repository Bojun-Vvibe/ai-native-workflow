# Bad sample 2: docker-compose mapping-form, APP_KEY=ChangeMeChangeMe.
services:
  app:
    image: fireflyiii/core:latest
    ports:
      - "8080:8080"
    environment:
      APP_URL: https://firefly.example.org
      SITE_OWNER: ops@example.org
      STATIC_CRON_TOKEN: 0123456789abcdef0123456789abcdef
      DB_CONNECTION: mysql
      DB_HOST: db
      DB_USERNAME: firefly
      DB_PASSWORD: firefly
      APP_KEY: ChangeMeChangeMeChangeMe
