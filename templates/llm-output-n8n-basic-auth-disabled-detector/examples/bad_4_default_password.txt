# LLM-emitted compose snippet that turned auth on but used the
# tutorial's literal placeholder password. Operators routinely ship
# this verbatim because the API responds 200 OK and the editor
# loads.
services:
  n8n:
    image: n8nio/n8n:latest
    environment:
      N8N_HOST: workflows.example.io
      N8N_BASIC_AUTH_ACTIVE: "true"
      N8N_BASIC_AUTH_USER: "admin"
      N8N_BASIC_AUTH_PASSWORD: "changeme"
      WEBHOOK_URL: "https://workflows.example.io/"
