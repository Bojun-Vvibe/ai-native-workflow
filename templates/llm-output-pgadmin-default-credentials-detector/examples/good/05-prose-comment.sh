# Documentation prose — mentions the default but does not assign it.
# Do NOT keep PGADMIN_DEFAULT_PASSWORD=admin from the upstream README.
# Set it to a real secret reference instead, e.g. ${PGADMIN_PW}.
echo "see README"
