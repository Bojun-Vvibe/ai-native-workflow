server:
  port: 8080
management:
  endpoints:
    web:
      exposure:
        include: health,info
        exclude: env,heapdump,shutdown
      cors:
        allowed-origins: https://ops.example.com
  endpoint:
    health:
      show-details: never
    shutdown:
      enabled: false
