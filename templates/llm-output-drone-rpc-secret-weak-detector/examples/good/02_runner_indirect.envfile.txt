# Secret injected from the host environment / vault — never literal in
# the file, so the detector cannot judge entropy. Treat as not-bad.
DRONE_RPC_SECRET=${DRONE_RPC_SECRET:?required}
DRONE_RUNNER_CAPACITY=2
DRONE_RPC_HOST=drone.example.org
DRONE_RPC_PROTO=https
