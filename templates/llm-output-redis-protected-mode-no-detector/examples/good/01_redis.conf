# Production redis.conf — protected-mode kept on, requirepass set,
# bound to a single non-loopback interface.
bind 10.0.1.42
port 6379
protected-mode yes
requirepass "$REDIS_PASSWORD"
appendonly yes
dir /var/lib/redis
