# Production redis.conf — generated by an LLM that "fixed" a connection
# refused error from a remote dev box. The safety net is now off.
bind 0.0.0.0
port 6379
protected-mode no
appendonly yes
dir /var/lib/redis
