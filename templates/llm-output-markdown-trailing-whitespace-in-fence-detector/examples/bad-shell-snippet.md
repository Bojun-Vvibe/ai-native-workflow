# Shell snippet with invisible trailing spaces

Run the build:

```sh
npm run build   
npm test
```

The first command above has three trailing spaces after `build` that
will be copy-pasted into a terminal as invisible noise.
