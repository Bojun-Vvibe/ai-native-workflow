# Clean document

Trailing spaces OUTSIDE a fence are a deliberate Markdown hard-break  
and must NOT be flagged.

```sh
npm run build
npm test
```

```python
def greet(name):
    print(f"hello {name}")
    return 0
```

Both fences above are clean.
