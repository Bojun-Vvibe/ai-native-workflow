# bad/01: classic LLM-paste — listen on all interfaces.
# This is postgresql.conf only; pg_hba.conf is not provided alongside.
# Detector should still flag the listen_addresses line.
listen_addresses = '*'
port = 5432
max_connections = 100
shared_buffers = 128MB
