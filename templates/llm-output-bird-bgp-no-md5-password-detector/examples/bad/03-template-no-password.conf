router id 10.0.0.3;

template bgp tpl_v4 {
    local as 65020;
    neighbor 203.0.113.7 as 64540;
    /* password "n0pe"; -- intentionally disabled during migration */
    import all;
    export none;
}

protocol bgp peer_v4 from tpl_v4 {
    description "ipv4 peer";
}
