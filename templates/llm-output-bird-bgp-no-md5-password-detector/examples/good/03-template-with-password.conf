router id 10.0.1.3;

# Template that defines the password; used by the active session.
template bgp tpl_secure {
    local as 65020;
    password "rotated-monthly";
    import all;
    export none;
}

protocol bgp peer_a from tpl_secure {
    neighbor 203.0.113.7 as 64540;
}
