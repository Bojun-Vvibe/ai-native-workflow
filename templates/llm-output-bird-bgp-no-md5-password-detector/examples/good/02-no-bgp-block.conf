router id 10.0.1.2;

# A direct (no neighbor / no session) protocol; nothing to authenticate.
protocol device { }
protocol kernel {
    ipv4 { export all; };
}
protocol static {
    ipv4 { import all; };
    route 10.0.0.0/8 via 10.0.1.254;
}
