[users]
admin = $shiro1$SHA-256$500000$abc==$def==, admin
analyst = $shiro1$SHA-256$500000$ghi==$jkl==, analyst

[roles]
admin = *
analyst = read

[urls]
/login = anon
/logout = logout
# Mid-chain anon is OK; final filter `authc` decides.
/api/notebook/** = anon, authc
/** = authc
