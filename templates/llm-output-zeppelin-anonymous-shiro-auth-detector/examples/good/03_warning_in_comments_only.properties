# Zeppelin hardening checklist
#
# DO NOT use `/** = anon` in conf/shiro.ini -- that exposes every
# notebook to unauthenticated users (CWE-306). Use `/** = authc`
# and pin role-restricted paths above it.
#
# Reference: Apache Zeppelin security docs.
