# Good sample 2: documentation/template stub that mentions
# WEBPASSWORD only inside a `#` comment, with no real install
# fingerprints. The detector should not fire here because the
# file is not actually a configured setupVars.conf.
# Example template — fill in the values:
#   WEBPASSWORD=
#   PIHOLE_INTERFACE=eth0
# These are illustrative only and are commented out so that
# pihole's installer prompts for them interactively.
