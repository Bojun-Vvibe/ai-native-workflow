# Good sample 4: not a setupVars.conf at all — looks like an
# unrelated env file that happens to define WEBPASSWORD as empty
# for some other tool. The detector requires Pi-hole-specific
# fingerprints, so this must not fire.
APP_ENV=production
DATABASE_URL=postgres://localhost/app
SESSION_SECRET=do-not-share
WEBPASSWORD=
