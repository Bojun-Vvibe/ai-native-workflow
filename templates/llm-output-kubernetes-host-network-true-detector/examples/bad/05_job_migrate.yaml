apiVersion: batch/v1
kind: Job
metadata:
  name: migrate
spec:
  template:
    spec:
      hostNetwork: on
      restartPolicy: Never
      containers:
      - name: migrate
        image: example/migrate:1.0
