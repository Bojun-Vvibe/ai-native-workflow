version: 0.1
storage:
  filesystem:
    rootdirectory: /var/lib/registry
http:
  addr: 127.0.0.1:5000
auth:
  token:
    realm: https://auth.example.internal/token
    service: container_registry
    issuer: registry-token-issuer
    rootcertbundle: /etc/registry/auth.crt
