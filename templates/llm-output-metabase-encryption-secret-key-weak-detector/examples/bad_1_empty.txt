# LLM-emitted compose snippet: encryption key wired through but
# left empty. Metabase boots, accepts the empty value, and writes
# every connected source's credentials to the app DB in cleartext.
services:
  metabase:
    image: metabase/metabase:v0.49.6
    ports:
      - "3000:3000"
    environment:
      MB_DB_TYPE: postgres
      MB_DB_HOST: pg
      MB_DB_PORT: 5432
      MB_DB_USER: metabase
      MB_DB_DBNAME: metabase
      MB_ENCRYPTION_SECRET_KEY: ""
