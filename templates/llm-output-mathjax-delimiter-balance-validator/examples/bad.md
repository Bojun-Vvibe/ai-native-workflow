# Math notes (broken)

This paragraph opens an inline math span $a + b but never closes it, so the
rest of the paragraph is treated as math by the renderer.

A separate paragraph with mismatched LaTeX brackets: \(x + y = z without
the closing paren.

A display block opens but never closes:

$$
\int f(x) \, dx

And a square bracket display opens \[ \alpha + \beta and trails off.

Final paragraph: balanced inline like $p$ and $q$ — fine on its own.
