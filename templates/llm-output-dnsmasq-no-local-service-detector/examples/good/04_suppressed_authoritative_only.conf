# dnsmasq-no-local-service-allowed
# This deployment is intentionally a public authoritative DNS island
# (no-resolv plus only address= rules); it never recurses.
no-resolv
no-hosts
address=/internal.example.test/10.0.0.5
address=/api.example.test/10.0.0.6
cache-size=0
