# Good sample

This line has *matched emphasis* on both ends.

This line has **matched strong** on both ends.

A line with *one* and **two** correctly paired emphasis tokens.

Inline code is ignored: `*not emphasis*` and `**also not**` here.

Escaped asterisks count as literal characters: 5 \* 3 = 15 and
3 \* 4 = 12.

A list with valid emphasis:

* first item with *good* emphasis
* second item with **good** strong
* third item, no emphasis at all

Triple combined: ***bold-italic*** correctly paired.

```text
This *has unmatched emphasis* and a stray *.
```

End of good sample.
