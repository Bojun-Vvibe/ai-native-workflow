# LLMs love this one: "just trust the private subnet".
# Anyone with VPC access (or who lands a pod on the same network) wins.
local   all             all                                     peer
host    all             all             10.0.0.0/8              trust
host    all             all             172.16.0.0/12           trust
host    all             all             192.168.0.0/16          trust
