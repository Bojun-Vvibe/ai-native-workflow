# Certificate auth on the private subnet — clients must present a cert
# signed by the configured CA. Not trust.
local   all             all                                     peer
hostssl all             all             10.0.0.0/8              cert clientcert=verify-full
hostssl all             all             0.0.0.0/0               cert
