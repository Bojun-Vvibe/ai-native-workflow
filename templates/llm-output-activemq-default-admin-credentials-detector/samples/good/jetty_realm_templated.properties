# jetty-realm.properties — credentials sourced from a secret store at deploy time.
# The literal "admin: admin" pair is gone; the ops user has a strong password.
ops: ${OPS_CONSOLE_PASSWORD}, admin
readonly: ${READONLY_CONSOLE_PASSWORD}, viewer
