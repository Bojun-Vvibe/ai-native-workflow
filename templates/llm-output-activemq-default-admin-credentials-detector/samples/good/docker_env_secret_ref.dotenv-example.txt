# docker-compose env fragment — admin login is renamed and the password is
# loaded from the orchestrator's secret store. Defaults removed.
ACTIVEMQ_ADMIN_LOGIN=ops_console
ACTIVEMQ_ADMIN_PASSWORD=${AMQ_CONSOLE_PASSWORD_FROM_SECRETS}
ACTIVEMQ_OPTS_MEMORY=-Xms512m -Xmx1g
