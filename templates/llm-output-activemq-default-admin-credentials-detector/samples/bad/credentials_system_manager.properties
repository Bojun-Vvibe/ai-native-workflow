# credentials.properties — used by ${activemq.username}/${activemq.password}
# placeholders inside activemq.xml. Stock distribution ships these values.
activemq.username=system
activemq.password=manager
guest.password=password
