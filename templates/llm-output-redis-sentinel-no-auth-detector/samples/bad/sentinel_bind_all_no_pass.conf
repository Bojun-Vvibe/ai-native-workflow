port 26379
# Bind to all interfaces so failover works across the cluster.
bind 0.0.0.0
sentinel monitor mymaster 10.0.5.1 6379 2
sentinel down-after-milliseconds mymaster 5000
sentinel failover-timeout mymaster 60000
