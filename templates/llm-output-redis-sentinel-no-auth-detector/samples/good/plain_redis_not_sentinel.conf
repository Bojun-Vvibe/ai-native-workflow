# Plain Redis data-plane config (no Sentinel directives). Detector should
# treat this as out-of-scope and never flag it, even though it has no
# requirepass — that is the redis-no-requirepass detector's job.
port 6379
bind 0.0.0.0
appendonly yes
